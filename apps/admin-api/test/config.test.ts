import {describe,expect,it}from"vitest";import{loadAdminApiConfiguration}from"../src/config";
const development={NODE_ENV:"development",ADMIN_AUTH_MODE:"password",ADMIN_ORIGIN:"http://localhost:3001",ADMIN_DEPLOYMENT_ID:"local",ADMIN_REGION:"nl"};const production={NODE_ENV:"production",ADMIN_AUTH_MODE:"password",ADMIN_ORIGIN:"https://admin.tikdd.example",ADMIN_DEPLOYMENT_ID:"tikdd",ADMIN_REGION:"nl",ADMIN_CSRF_SECRET:"csrf-secret-with-at-least-32-characters",ADMIN_COMMAND_SECRET:"command-secret-with-at-least-32-characters",ADMIN_ORIGIN_PROOF:"origin-proof-with-at-least-32-characters",WEB_ORIGIN:"https://tikdd.example",PUBLIC_CONTENT_REVALIDATION_SECRET:"content-revalidation-secret-at-least-32-bytes"};
describe("Admin API configuration",()=>{it("requires password auth and loopback API hosting",()=>{expect(loadAdminApiConfiguration(development)).toMatchObject({host:"127.0.0.1",auth:{mode:"password",originProof:null}});expect(()=>loadAdminApiConfiguration({...development,ADMIN_API_HOST:"0.0.0.0"})).toThrow("loopback-only");expect(()=>loadAdminApiConfiguration({...development,ADMIN_AUTH_MODE:"development"})).toThrow("must be password");});it("requires the production HTTPS origin and origin proof",()=>{expect(loadAdminApiConfiguration(production).auth.mode).toBe("password");expect(()=>loadAdminApiConfiguration({...production,ADMIN_ORIGIN_PROOF:"short"})).toThrow("ADMIN_ORIGIN_PROOF");expect(()=>loadAdminApiConfiguration({...production,ADMIN_ORIGIN:"http://admin.tikdd.example"})).toThrow("HTTPS");});});
