import { describe,expect,it } from "vitest";import { hashAdminPassword,passwordHashNeedsUpgrade,validateNewPassword,verifyAdminPassword } from "../src/auth";
describe("administrator password boundary",()=>{it("hashes and verifies a strong password without storing it",async()=>{const password="correct horse battery staple";const hash=await hashAdminPassword("owner",password);expect(hash).toMatch(/^scrypt\$v1\$/);expect(hash).not.toContain(password);await expect(verifyAdminPassword(password,hash)).resolves.toBe(true);await expect(verifyAdminPassword("different password",hash)).resolves.toBe(false);expect(passwordHashNeedsUpgrade(hash)).toBe(false);});it("rejects weak credentials and performs dummy verification",async()=>{expect(()=>validateNewPassword("administrator","administrator")).toThrow();expect(()=>validateNewPassword("owner","short")).toThrow();await expect(verifyAdminPassword("some invalid credential",null)).resolves.toBe(false);});});
