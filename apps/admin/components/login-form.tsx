"use client";
import { ArrowRight,DownloadSimple,GlobeHemisphereWest,LockKey,ShieldCheck,WarningCircle } from "@phosphor-icons/react";import { useState } from "react";import { useRouter } from "next/navigation";
export function LoginForm(){const router=useRouter();const[username,setUsername]=useState("");const[password,setPassword]=useState("");const[message,setMessage]=useState("");const[busy,setBusy]=useState(false);const submit=async(event:React.FormEvent)=>{event.preventDefault();setBusy(true);setMessage("");try{const response=await fetch("/api/admin/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({username,password})});const data=await response.json();if(!response.ok){setMessage(data.error??"无法登录");return;}router.replace("/");router.refresh();}catch{setMessage("认证服务暂不可用，请检查 Admin API、Postgres 与 Redis。");}finally{setBusy(false);}};return <main className="login-shell"><section className="login-boundary"><a className="login-brand" href="/login"><span><DownloadSimple size={21} weight="bold"/></span><b>TikDD</b><small>OWNER CONTROL</small></a><div className="boundary-copy"><p>PRIVATE CONTROL ENTRY</p><h1>站点运行，<br/>从可信边界开始。</h1><span>管理 Provider 路由、多语言内容与发布快照。公众下载服务与这里保持完全解耦。</span></div><div className="boundary-map"><div><GlobeHemisphereWest/><span><b>NL</b><small>荷兰实例</small></span></div><ArrowRight/><div><ShieldCheck/><span><b>Loopback</b><small>私有 Admin API</small></span></div><ArrowRight/><div><LockKey/><span><b>Owner</b><small>单管理员会话</small></span></div></div><footer><ShieldCheck size={17}/>凭据不会进入浏览器存储、URL 或公共 API。</footer></section><section className="login-panel"><form onSubmit={submit}><header><span><LockKey size={22}/></span><div><small>ADMINISTRATOR</small><h2>登录站长后台</h2></div></header><p>使用本机初始化的管理员账号继续。</p>{message?<div className="login-error" role="alert"><WarningCircle size={18}/>{message}</div>:null}<label>用户名<input autoComplete="username" autoCapitalize="none" spellCheck={false} minLength={3} maxLength={64} required value={username} onChange={event=>setUsername(event.target.value)}/></label><label>密码<input type="password" autoComplete="current-password" minLength={8} maxLength={128} required value={password} onChange={event=>setPassword(event.target.value)}/></label><button disabled={busy}>{busy?"正在验证…":<>进入控制台 <ArrowRight size={17}/></>}</button><small className="session-note">空闲 30 分钟后失效 · 最长 12 小时</small></form></section></main>}
